name: Metrics
on:
  schedule: [{cron: "0 0 * * *"}]
  workflow_dispatch:
  push: {branches: ["main"]}
jobs:
  github-metrics:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: lowlighter/metrics@latest
        with:
          token: ${{ secrets.METRICS_TOKEN }}
          filename: github-metrics1.svg
          
          # 기본 통계 제거
          base: ""
          
          # 언어 설정 (파이 차트 강제 적용)
          plugin_languages: yes
          plugin_languages_ignored: html, css, makefile
          
          # 👇 핵심 변경: donut 대신 pie 사용
          plugin_languages_layout: pie
          
          # 퍼센트 표시
          plugin_languages_details: percentage
          plugin_languages_limit: 8
