name: Metrics
on:
  schedule: [{cron: "0 0 * * *"}]
  workflow_dispatch:
  push: {branches: ["master", "main"]}
jobs:
  github-metrics:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: lowlighter/metrics@latest
        with:
          token: ${{ secrets.METRICS_TOKEN }}
          
          # 👇 파일 이름을 아예 새로 지어서 캐시 문제 원천 차단
          filename: my-final-metrics.svg
          
          # 기본 통계 끄기
          base: ""
          
          # 언어 설정 (도넛 + 8개 + 퍼센트)
          plugin_languages: yes
          plugin_languages_ignored: html, css, makefile
          
          # 레이아웃: 도넛
          plugin_languages_layout: donut
          plugin_languages_layout_donut_size: 50%
          
          # 텍스트: 퍼센트만 표시
          plugin_languages_details: percentage
          
          # 개수: 8개까지
          plugin_languages_limit: 8
